import styled from "styled-components";

import { colors } from "../../../style";

const LoginPromoTitle = styled.h1`
  margin: 0;
  font-weight: bold;
  font-size: 24px;
  line-height: 40px;
  color: ${colors.darkBlack};
`;

export { LoginPromoTitle };
